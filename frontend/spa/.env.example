# База для axios (пусто = относительный префикс /api за тем же Nginx)
# VITE_API_BASE_URL=

# Цель прокси Vite для /api и /voice (только dev). В Docker: http://web:8000 (см. docker-compose.yml)
# VITE_PROXY_TARGET=http://127.0.0.1:8000
