; HTTP-сервер для ARI (REST + WebSocket событий).
; # TODO: В продакшене ограничьте доступ к порту 8088 файрволом / Docker-сетью.

[general]
enabled=yes
bindaddr=0.0.0.0
bindport=8088
enablestatic=no
